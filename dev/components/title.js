import {h, Component} from 'composi'

export default new Component({
  container: 'header',
  render: (message) => {
    return (
      <nav>
        <h1>Composi Image Browser</h1>
      </nav>
    )
  }
})
